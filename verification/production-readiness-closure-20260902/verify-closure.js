const fs=require('node:fs'),path=require('node:path');
const root=path.resolve('.'),dir=path.join(root,'verification/production-readiness-closure-20260902');
const required=['production-resource-manifest.json','production-secret-inventory.json','backup-policy.json','monitoring-alert-policy.json','dns-tls-ingress-plan.md','production-smoke-contract.md','production-rollback-runbook.md','production-deployment-runbook.md','human-approval-matrix.md','expected-schema-fingerprint.json','migration-manifest.json'];
const missing=required.filter(f=>!fs.existsSync(path.join(dir,f))); const m=JSON.parse(fs.readFileSync(path.join(dir,'production-resource-manifest.json'))); const fp=JSON.parse(fs.readFileSync(path.join(dir,'expected-schema-fingerprint.json'))); const mig=JSON.parse(fs.readFileSync(path.join(dir,'migration-manifest.json'))); const order=mig.map(x=>x.file); const secrets=JSON.stringify(JSON.parse(fs.readFileSync(path.join(dir,'production-secret-inventory.json')))); const leaks=/(eyJ[A-Za-z0-9_-]{20,}|meoo_project_service_v1|SUPABASE_SERVICE_ROLE_KEY=|MEOO_PROJECT_API_KEY=)/.test(secrets);
const result={requiredArtifacts:missing.length===0,migrationOrder:order.length===12&&order[10]==='011_ai_template_studio.sql'&&order[11]==='012_launch_v1_domains.sql',expectedLatestMigration:m.expectedLatestMigration==='012_launch_v1_domains',schemaDigest:fp.digest,secretInventoryNoValues:!leaks,productionProjectUnbound:m.productionProjectId===null,stagingProjectId:m.stagingProjectId}; result.pass=Object.values(result).filter(v=>typeof v==='boolean').every(Boolean); console.log(JSON.stringify(result,null,2)); process.exitCode=result.pass?0:1;
